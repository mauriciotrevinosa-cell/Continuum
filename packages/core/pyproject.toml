[project]
name = "continuum-core"
version = "0.0.0"
description = "Domain primitives: ids, time axes, errors, hashing. No I/O."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
